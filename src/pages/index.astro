---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
---

<Layout title="Burrow - Home" description="Burrow project case study and documentation">
  <Navigation />
  <main class="gradient-background">
    <div class="container">
      <section class="hero">
        <div class="hero-content">
          <div class="hero-logo">
            <img src={`${import.meta.env.BASE_URL}nut-with-doc.png`} alt="Burrow Logo" class="hero-logo-img" />
          </div>
          <div class="hero-text">
            <h1 class="hero-title">Burrow</h1>
            <p class="tagline">
              A self-hosted data ingestion pipeline and retrieval API that streamlines and accelerates the development of RAG applications
            </p>
            <div class="hero-buttons">
              <a href="/case-study" class="btn-primary">Read the Case Study</a>
              <a href="https://github.com/burrow-io" class="btn-secondary">Download from GitHub</a>
            </div>
          </div>
        </div>
      </section>
    </div>
  </main>

  <!-- White background sections -->
  <section class="content-sections">
    <div class="container">
      <section class="feature-section feature-with-image feature-alternate">
        <div class="feature-image feature-image-video">
          <video src={`${import.meta.env.BASE_URL}vid-burrow.mp4`} controls autoplay loop muted class="feature-video">
            Your browser does not support the video tag.
          </video>
        </div>
        <div class="feature-content">
          <h2>Automated Ingestion Pipeline</h2>
          <p>
            Ease of getting documents into a database via both friendly <span class="highlight-emphasis">GUI</span> and <span class="highlight-emphasis">RESTful endpoints</span>, the pipeline takes care of the rest.
          </p>
        </div>
      </section>

      <section class="feature-section feature-with-image">
        <div class="feature-content">
          <h2>Intelligent Document Processing & Retrieval</h2>
          <p>
            <span class="highlight-emphasis">Layout-aware</span> document processing and <span class="highlight-emphasis">structure-preserving</span> chunking, combined with a production-grade RAG API offering <span class="highlight-emphasis">hybrid search</span>, <span class="highlight-emphasis">reranking</span>, and <span class="highlight-emphasis">metadata filtering</span>.
          </p>
        </div>
        <div class="feature-visual">
          <div class="processing-flow">
            <div class="processing-circle">
              <div class="processing-group">
                <div class="flow-step">
                  <img src={`${import.meta.env.BASE_URL}parsing.png`} alt="Parsing" class="flow-image" />
                  <div class="flow-label">Parsing</div>
                </div>
                <div class="flow-arrow">→</div>
                <div class="flow-step">
                  <img src={`${import.meta.env.BASE_URL}chunking.png`} alt="Chunking" class="flow-image" />
                  <div class="flow-label">Chunking</div>
                </div>
                <div class="flow-arrow">→</div>
                <div class="flow-step">
                  <img src={`${import.meta.env.BASE_URL}embedding.png`} alt="Embedding" class="flow-image" />
                  <div class="flow-label">Embedding</div>
                </div>
              </div>
            </div>
            <div class="arrow-to-storage">
              <div class="arrow-down"></div>
            </div>
            <div class="flow-step storage-step">
              <img src={`${import.meta.env.BASE_URL}storage.png`} alt="Vector Store" class="flow-image" />
              <div class="flow-label">Vector Store</div>
            </div>
            <div class="storage-to-badges">
              <div class="arrow-from-storage"></div>
            </div>
            <div class="feature-badges">
              <span class="badge badge-emphasized">Hybrid Search</span>
              <span class="badge badge-emphasized">Reranking</span>
              <span class="badge badge-emphasized">Metadata Filtering</span>
            </div>
          </div>
        </div>
      </section>

      <section class="feature-section feature-with-image feature-alternate">
        <div class="feature-image feature-image-cli">
          <img src={`${import.meta.env.BASE_URL}cli-ascii.png`} alt="Burrow CLI Screenshot" class="cli-image" />
        </div>
        <div class="feature-content">
          <h2>Simplified AWS Deployment Using the Burrow CLI</h2>
          <p>
            Automated deployment to your AWS environment using a <span class="highlight-emphasis">self-hosted</span>, <span class="highlight-emphasis">serverless architecture</span> where performance and cost <span class="highlight-emphasis">scale</span> based on usage patterns.
          </p>
        </div>
      </section>

      <section class="feature-section next-steps">
        <h2>Next Steps</h2>
        <p>
          Ready to dive deeper into Burrow? Explore our comprehensive case study or download from GitHub.
        </p>
        <div class="next-steps-buttons">
          <a href="/case-study" class="btn-primary">Case Study</a>
          <a href="https://github.com/burrow-io" target="_blank" rel="noopener noreferrer" class="btn-secondary">Download from GitHub</a>
        </div>
      </section>
    </div>
  </section>
</Layout>

<style>
.hero-buttons {
  display: flex;
  gap: 1.25rem;
  justify-content: flex-start;
  margin-top: 2.5rem;
}

.btn-primary, .btn-secondary {
  padding: 1rem 2.25rem;
  text-decoration: none;
  border-radius: 12px;
  font-weight: 600;
  font-size: 1rem;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
  box-shadow: 0 4px 14px rgba(0, 0, 0, 0.1);
}

.btn-primary {
  background: linear-gradient(135deg, var(--brand-teal) 0%, #0080a8 100%);
  color: white;
  border: none;
}

.btn-primary::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
  transition: left 0.5s;
}

.btn-primary:hover::before {
  left: 100%;
}

.btn-primary:hover {
  transform: translateY(-3px);
  box-shadow: 0 8px 24px rgba(0, 105, 148, 0.4);
}

.btn-secondary {
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(10px);
  border: 2px solid rgba(0, 105, 148, 0.3);
  color: var(--text-dark);
}

.btn-secondary:hover {
  background: rgba(0, 105, 148, 0.1);
  border-color: var(--brand-teal);
  transform: translateY(-3px);
  box-shadow: 0 8px 24px rgba(0, 105, 148, 0.2);
}

/* Content sections */
.content-sections {
  background: linear-gradient(
    to bottom,
    rgba(255, 255, 255, 0.95) 0%,
    rgba(248, 250, 252, 0.98) 30%,
    rgba(255, 255, 255, 1) 100%
  );
  padding: 0;
  position: relative;
  transition: background-color 0.3s ease, background-image 0.3s ease;
}

.content-sections .container {
  display: flex;
  flex-direction: column;
  gap: 6rem;
  padding: 6rem 2rem;
  background: transparent;
}

.content-sections::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(0, 105, 148, 0.15), transparent);
  transition: background 0.3s ease;
}

.feature-section {
  padding: 0;
  max-width: 800px;
  margin: 0 auto;
  text-align: center;
  position: relative;
}

.feature-with-image {
  max-width: 1500px;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 5rem;
  align-items: center;
  text-align: left;
  width: 100%;
}

/* Ensure content comes first, visual second on all screen sizes */
.feature-content {
  order: 1;
}

.feature-image,
.feature-visual {
  order: 2;
}

/* Alternate layout: visual/image on left, content on right for larger screens */
.feature-alternate .feature-visual,
.feature-alternate .feature-image {
  order: 1;
}

.feature-alternate .feature-content {
  order: 2;
}

.feature-content {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  position: relative;
  z-index: 1;
}

.feature-section h2 {
  font-size: 2.5rem;
  color: var(--text-dark);
  margin-bottom: 1.5rem;
  font-weight: 700;
  letter-spacing: -0.03em;
  line-height: 1.2;
  position: relative;
  padding-bottom: 1rem;
  background: linear-gradient(135deg, var(--text-dark) 0%, rgba(0, 105, 148, 0.8) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  transition: all 0.3s ease;
}

.feature-content:hover .feature-section h2 {
  background: linear-gradient(135deg, var(--text-dark) 0%, var(--brand-teal) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.feature-section h2::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  width: 60px;
  height: 3px;
  background: linear-gradient(90deg, var(--brand-teal), rgba(166, 124, 82, 0.6));
  border-radius: 2px;
  transition: width 0.3s ease;
}

.feature-content:hover .feature-section h2::after {
  width: 100px;
}

.feature-section p {
  font-size: 1.3rem;
  color: var(--text-muted);
  line-height: 2;
  margin: 0;
  font-weight: 400;
}

.highlight-emphasis {
  color: #006994 !important;
  font-weight: 700;
}
  letter-spacing: 0.01em;
  text-shadow: 0 1px 3px rgba(0, 0, 0, 0.05);
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
}

.feature-content:hover .feature-section p {
  color: rgba(0, 0, 0, 0.85);
  text-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
  transform: translateY(-1px);
}

.feature-image {
  text-align: center;
  padding: 0;
  background: transparent;
  border-radius: 0;
  position: relative;
  overflow: visible;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  display: flex;
  align-items: center;
  justify-content: center;
}

.feature-image img:hover,
.feature-image video:hover {
  transform: translateY(-12px) scale(1.02);
  box-shadow: 
    0 32px 80px rgba(0, 0, 0, 0.18),
    0 12px 32px rgba(0, 0, 0, 0.12),
    0 0 0 1px rgba(255, 255, 255, 0.6) inset;
  filter: brightness(1.05) contrast(1.02);
}


.feature-image img {
  max-width: 65%;
  height: auto;
  border-radius: 20px;
  border: 1px solid rgba(0, 0, 0, 0.06);
  box-shadow: 
    0 20px 60px rgba(0, 0, 0, 0.12),
    0 8px 24px rgba(0, 0, 0, 0.08),
    0 0 0 1px rgba(255, 255, 255, 0.5) inset;
  transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  z-index: 1;
  background: white;
  display: block;
  filter: brightness(1.02) contrast(1.01);
}

.feature-image-cli {
  position: relative;
  padding: 1.5rem;
  background: linear-gradient(135deg, rgba(0, 105, 148, 0.08) 0%, rgba(166, 124, 82, 0.08) 50%, rgba(0, 105, 148, 0.06) 100%);
  border-radius: 24px;
  backdrop-filter: blur(12px);
  max-width: 75%;
  margin: 0 auto;
}

.feature-image-cli::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  border-radius: 24px;
  background: radial-gradient(circle at 30% 30%, rgba(0, 105, 148, 0.12) 0%, transparent 60%);
  z-index: 0;
  pointer-events: none;
}

.cli-image {
  max-width: 85% !important;
  border-radius: 16px;
  border: 2px solid rgba(255, 255, 255, 0.8);
  box-shadow: 
    0 24px 72px rgba(0, 0, 0, 0.15),
    0 12px 32px rgba(0, 0, 0, 0.1),
    0 4px 12px rgba(0, 0, 0, 0.08),
    0 0 0 1px rgba(0, 105, 148, 0.1),
    inset 0 1px 0 rgba(255, 255, 255, 0.9);
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.98) 0%, rgba(248, 250, 252, 0.95) 100%);
  position: relative;
  z-index: 1;
  filter: brightness(1.03) contrast(1.02) saturate(1.05);
  transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}

.feature-image-cli:hover {
  transform: translateY(-4px);
}

.feature-image-cli:hover .cli-image {
  transform: translateY(-8px) scale(1.01);
  box-shadow: 
    0 32px 96px rgba(0, 0, 0, 0.2),
    0 16px 48px rgba(0, 0, 0, 0.14),
    0 8px 20px rgba(0, 0, 0, 0.1),
    0 0 0 1px rgba(0, 105, 148, 0.15),
    inset 0 1px 0 rgba(255, 255, 255, 1);
  filter: brightness(1.05) contrast(1.03) saturate(1.08);
}

.feature-image-cli:hover::before {
  background: radial-gradient(circle at 30% 30%, rgba(0, 105, 148, 0.18) 0%, transparent 60%);
}

.feature-image video {
  max-width: 90%;
  height: auto;
  border-radius: 20px;
  border: none;
  box-shadow: 
    0 20px 60px rgba(0, 0, 0, 0.15),
    0 8px 24px rgba(0, 0, 0, 0.1),
    0 0 0 1px rgba(255, 255, 255, 0.1) inset;
  transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  z-index: 1;
  background: #000;
  display: block;
  margin: 0 auto;
  overflow: hidden;
}

.feature-image-video {
  position: relative;
  padding: 1.5rem;
  background: linear-gradient(135deg, rgba(0, 105, 148, 0.08) 0%, rgba(166, 124, 82, 0.08) 50%, rgba(0, 105, 148, 0.06) 100%);
  border-radius: 24px;
  backdrop-filter: blur(12px);
  max-width: 95%;
  margin: 0 auto;
}

.feature-image-video::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  border-radius: 24px;
  background: radial-gradient(circle at 30% 30%, rgba(0, 105, 148, 0.12) 0%, transparent 60%);
  z-index: 0;
  pointer-events: none;
}

.feature-image-video .feature-video {
  max-width: 100% !important;
  border-radius: 16px;
  border: 2px solid rgba(255, 255, 255, 0.8);
  box-shadow: 
    0 24px 72px rgba(0, 0, 0, 0.15),
    0 12px 32px rgba(0, 0, 0, 0.1),
    0 4px 12px rgba(0, 0, 0, 0.08),
    0 0 0 1px rgba(0, 105, 148, 0.1),
    inset 0 1px 0 rgba(255, 255, 255, 0.9);
  position: relative;
  z-index: 1;
  filter: brightness(1.03) contrast(1.02) saturate(1.05);
  transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}

.feature-image-video:hover {
  transform: translateY(-4px);
}

.feature-image-video:hover .feature-video {
  transform: translateY(-8px) scale(1.01);
  box-shadow: 
    0 32px 96px rgba(0, 0, 0, 0.2),
    0 16px 48px rgba(0, 0, 0, 0.14),
    0 8px 20px rgba(0, 0, 0, 0.1),
    0 0 0 1px rgba(0, 105, 148, 0.15),
    inset 0 1px 0 rgba(255, 255, 255, 1);
  filter: brightness(1.05) contrast(1.03) saturate(1.08);
}

.feature-image-video:hover::before {
  background: radial-gradient(circle at 30% 30%, rgba(0, 105, 148, 0.18) 0%, transparent 60%);
}


.feature-video {
  display: block;
  width: 100%;
}

.feature-visual {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 2.5rem;
  padding: 0;
  width: 100%;
  background: transparent;
  border-radius: 0;
  position: relative;
  overflow: visible;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}


.processing-flow {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  padding: 2rem 1rem;
  background: linear-gradient(135deg, rgba(0, 105, 148, 0.15) 0%, rgba(166, 124, 82, 0.15) 50%, rgba(0, 105, 148, 0.12) 100%);
  border-radius: 24px;
  border: 1px solid rgba(0, 0, 0, 0.06);
  box-shadow: 
    0 20px 60px rgba(0, 0, 0, 0.12),
    0 8px 24px rgba(0, 0, 0, 0.08),
    0 0 0 1px rgba(255, 255, 255, 0.5) inset,
    0 1px 2px rgba(0, 0, 0, 0.04) inset;
  width: 100%;
  max-width: 100%;
  position: relative;
  z-index: 1;
  transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1), box-shadow 0.5s cubic-bezier(0.4, 0, 0.2, 1), background 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  overflow: hidden;
}

.processing-flow::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: radial-gradient(circle at 30% 30%, rgba(0, 105, 148, 0.2) 0%, transparent 50%);
  pointer-events: none;
  z-index: 0;
  transition: background 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}

.processing-flow:hover {
  transform: translateY(-8px);
  box-shadow: 
    0 32px 80px rgba(0, 0, 0, 0.18),
    0 12px 32px rgba(0, 0, 0, 0.12),
    0 0 0 1px rgba(255, 255, 255, 0.6) inset,
    0 1px 2px rgba(0, 0, 0, 0.06) inset;
}


.flow-top-row {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 1.5rem;
  flex-wrap: wrap;
  width: 100%;
}

.processing-circle {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 1.75rem 2.5rem;
  background: linear-gradient(135deg, rgba(255, 235, 59, 0.35) 0%, rgba(255, 245, 157, 0.3) 25%, rgba(255, 193, 7, 0.28) 50%, rgba(255, 245, 157, 0.3) 75%, rgba(255, 235, 59, 0.35) 100%);
  border-radius: 24px;
  border: 2px solid rgba(255, 193, 7, 0.4);
  box-shadow: 
    0 8px 24px rgba(255, 193, 7, 0.2),
    0 4px 12px rgba(255, 235, 59, 0.15),
    0 2px 6px rgba(0, 0, 0, 0.06),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  width: fit-content;
  margin: 0 auto;
  transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1), box-shadow 0.4s cubic-bezier(0.4, 0, 0.2, 1), background 0.4s cubic-bezier(0.4, 0, 0.2, 1), border-color 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
}

.processing-circle::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  border-radius: 24px;
  background: radial-gradient(circle at 30% 30%, rgba(255, 235, 59, 0.35) 0%, transparent 60%);
  z-index: 0;
  pointer-events: none;
}

.processing-circle::after {
  content: '';
  position: absolute;
  top: -50%;
  right: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(circle, rgba(255, 193, 7, 0.1) 0%, transparent 70%);
  opacity: 0.4;
  z-index: 0;
  pointer-events: none;
}

.processing-circle:hover {
  transform: translateY(-4px);
  box-shadow: 
    0 12px 32px rgba(255, 193, 7, 0.25),
    0 6px 16px rgba(255, 235, 59, 0.2),
    0 3px 8px rgba(0, 0, 0, 0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.7);
  background: linear-gradient(135deg, rgba(255, 235, 59, 0.45) 0%, rgba(255, 245, 157, 0.4) 25%, rgba(255, 193, 7, 0.38) 50%, rgba(255, 245, 157, 0.4) 75%, rgba(255, 235, 59, 0.45) 100%);
  border-color: rgba(255, 193, 7, 0.5);
}

.processing-circle:hover::before {
  background: radial-gradient(circle at 30% 30%, rgba(255, 235, 59, 0.4) 0%, transparent 60%);
}

.processing-circle:hover::after {
  opacity: 0.5;
}

.processing-group {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.75rem;
  flex-wrap: nowrap;
}

.processing-group .flow-step {
  padding: 0.75rem;
  min-width: 70px;
}

.processing-group .flow-image {
  width: 36px;
  height: 36px;
}

.processing-group .flow-label {
  font-size: 0.75rem;
}

.processing-group .flow-arrow {
  font-size: 1.2rem;
}

.arrow-to-storage {
  display: flex;
  justify-content: center;
  width: 100%;
  margin: 0.25rem 0;
}

.arrow-down {
  width: 2px;
  height: 40px;
  background: var(--brand-teal);
  position: relative;
}

.arrow-down::after {
  content: '';
  position: absolute;
  bottom: -6px;
  left: 50%;
  transform: translateX(-50%);
  width: 0;
  height: 0;
  border-left: 6px solid transparent;
  border-right: 6px solid transparent;
  border-top: 8px solid var(--brand-teal);
}

.flow-step {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.5rem;
  padding: 1.25rem;
  background: rgba(255, 255, 255, 0.98);
  backdrop-filter: blur(12px);
  border-radius: 16px;
  border: 1px solid rgba(0, 0, 0, 0.08);
  box-shadow: 
    0 8px 24px rgba(0, 0, 0, 0.1),
    0 4px 12px rgba(0, 0, 0, 0.06),
    0 0 0 1px rgba(255, 255, 255, 0.6) inset;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  min-width: 100px;
}

.flow-step:hover {
  transform: translateY(-6px) scale(1.03);
  box-shadow: 
    0 16px 40px rgba(0, 0, 0, 0.14),
    0 8px 20px rgba(0, 0, 0, 0.1),
    0 0 0 1px rgba(255, 255, 255, 0.8) inset;
  background: rgba(255, 255, 255, 1);
  border-color: rgba(0, 0, 0, 0.12);
}

.flow-icon {
  font-size: 2.5rem;
  line-height: 1;
}

.flow-image {
  width: 48px;
  height: 48px;
  object-fit: contain;
  filter: brightness(0) saturate(100%) invert(27%) sepia(8%) saturate(2000%) hue-rotate(180deg) brightness(0.3);
}

.storage-step {
  padding: 0.75rem !important;
  min-width: 75px !important;
}

.storage-step .flow-image {
  filter: brightness(0) saturate(100%) invert(27%) sepia(8%) saturate(2000%) hue-rotate(180deg) brightness(0.3);
  position: relative;
  width: 32px !important;
  height: 32px !important;
}

.flow-label {
  font-size: 0.9rem;
  font-weight: 600;
  color: var(--text-dark);
  text-align: center;
}

.flow-arrow {
  font-size: 1.5rem;
  color: var(--brand-teal);
  font-weight: bold;
}

.retrieval-features {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1.5rem;
  position: relative;
  width: 100%;
}

.storage-arrow-container {
  position: relative;
  width: 100%;
  display: flex;
  justify-content: center;
}

.arrow-line {
  width: 2px;
  height: 40px;
  background: linear-gradient(180deg, var(--brand-teal), transparent);
  position: relative;
}

.arrow-line::after {
  content: '';
  position: absolute;
  bottom: -6px;
  left: 50%;
  transform: translateX(-50%);
  width: 0;
  height: 0;
  border-left: 6px solid transparent;
  border-right: 6px solid transparent;
  border-top: 8px solid var(--brand-teal);
}

.storage-to-badges {
  display: flex;
  justify-content: center;
  width: 100%;
  position: relative;
  margin: 0.25rem 0 1rem 0;
}

.arrow-from-storage {
  width: 2px;
  height: 30px;
  background: linear-gradient(180deg, var(--brand-teal), var(--brand-teal));
  position: relative;
  margin: 0 auto;
}

.arrow-from-storage::after {
  content: '';
  position: absolute;
  bottom: -6px;
  left: 50%;
  transform: translateX(-50%);
  width: 0;
  height: 0;
  border-left: 6px solid transparent;
  border-right: 6px solid transparent;
  border-top: 8px solid var(--brand-teal);
}

.feature-badges {
  display: flex;
  gap: 1rem;
  flex-wrap: nowrap;
  justify-content: center;
  position: relative;
  width: 100%;
}

.badge-item {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.5rem;
}

.arrow-to-badge {
  width: 2px;
  height: 20px;
  background: var(--brand-teal);
  position: relative;
}

.arrow-to-badge::after {
  content: '';
  position: absolute;
  bottom: -4px;
  left: 50%;
  transform: translateX(-50%);
  width: 0;
  height: 0;
  border-left: 4px solid transparent;
  border-right: 4px solid transparent;
  border-top: 6px solid var(--brand-teal);
}

.badge {
  padding: 0.75rem 1.5rem;
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(0, 105, 148, 0.2);
  border-radius: 20px;
  font-size: 1rem;
  font-weight: 600;
  color: var(--text-dark);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  white-space: nowrap;
  box-shadow: 
    0 4px 16px rgba(0, 105, 148, 0.1),
    0 2px 8px rgba(0, 0, 0, 0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.8);
  position: relative;
}

.badge::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  border-radius: 20px;
  background: linear-gradient(135deg, rgba(0, 105, 148, 0.08), rgba(166, 124, 82, 0.08));
  opacity: 0;
  transition: opacity 0.3s ease;
  z-index: -1;
}

.badge:hover {
  transform: translateY(-4px);
  border-color: var(--brand-teal);
  box-shadow: 
    0 8px 24px rgba(0, 105, 148, 0.2),
    0 4px 12px rgba(0, 0, 0, 0.12),
    inset 0 1px 0 rgba(255, 255, 255, 0.9);
}

.badge:hover::before {
  opacity: 1;
}

.badge-emphasized {
  padding: 0.6rem 1.25rem;
  font-size: 0.95rem;
  font-weight: 700;
  background: linear-gradient(135deg, rgba(255, 235, 59, 0.4) 0%, rgba(255, 245, 157, 0.35) 30%, rgba(255, 235, 59, 0.3) 60%, rgba(255, 193, 7, 0.4) 100%);
  border: 2px solid rgba(255, 193, 7, 0.5);
  box-shadow: 
    0 8px 24px rgba(255, 193, 7, 0.2),
    0 4px 12px rgba(255, 235, 59, 0.15),
    0 2px 6px rgba(0, 0, 0, 0.1),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  transform: scale(1);
  color: var(--text-dark);
}

.badge-emphasized::before {
  background: linear-gradient(135deg, rgba(255, 235, 59, 0.5), rgba(255, 193, 7, 0.4));
  opacity: 0.6;
}

.badge-emphasized:hover {
  transform: translateY(-4px) scale(1.03);
  border-color: rgba(255, 193, 7, 0.7);
  background: linear-gradient(135deg, rgba(255, 235, 59, 0.5) 0%, rgba(255, 245, 157, 0.45) 30%, rgba(255, 235, 59, 0.4) 60%, rgba(255, 193, 7, 0.5) 100%);
  box-shadow: 
    0 12px 32px rgba(255, 193, 7, 0.3),
    0 6px 16px rgba(255, 235, 59, 0.2),
    0 3px 8px rgba(0, 0, 0, 0.12),
    inset 0 1px 0 rgba(255, 255, 255, 0.8);
}

.badge-emphasized:hover::before {
  opacity: 1;
}

.next-steps {
  border-top: 2px solid rgba(166, 124, 82, 0.1);
  padding-top: 4rem;
}

.next-steps-buttons {
  display: flex;
  gap: 1.5rem;
  justify-content: center;
  margin-top: 2rem;
}

.next-steps-buttons .btn-primary {
  background: var(--brand-teal);
  color: white;
  padding: 1rem 2rem;
  text-decoration: none;
  border-radius: 8px;
  font-weight: 500;
  transition: all 0.2s;
}

.next-steps-buttons .btn-secondary {
  border: 2px solid var(--brand-teal);
  color: var(--text-dark);
  padding: 1rem 2rem;
  text-decoration: none;
  border-radius: 8px;
  font-weight: 500;
  transition: all 0.2s;
}

.next-steps-buttons .btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(0, 105, 148, 0.4);
}

.next-steps-buttons .btn-secondary:hover {
  background: var(--brand-teal);
  color: white;
}

.hero-content {
  display: grid;
  grid-template-columns: 420px 1fr;
  gap: 2rem;
  align-items: center;
  justify-items: center;
  max-width: 1600px;
  margin: 0 auto;
  position: relative;
  z-index: 1;
}

.hero-text {
  text-align: left;
  position: relative;
  animation: fadeInUp 0.8s ease-out;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.hero-text h1 {
  margin-bottom: 1rem;
}

.hero-title {
  font-size: 6.5rem;
  font-weight: 485;
  color: #4e342e;
  display: inline-block;
  margin: 0;
  letter-spacing: -0.08em;
  font-family: 'Poppins', sans-serif;
  position: relative;
  text-shadow: 0 2px 20px rgba(78, 52, 46, 0.15);
  transition: transform 0.3s ease;
}

.hero-title::after {
  content: '';
  position: absolute;
  bottom: -12px;
  left: 0;
  width: 120px;
  height: 4px;
  background: linear-gradient(90deg, var(--brand-teal), var(--brand-primary));
  border-radius: 2px;
  opacity: 0.7;
}

.tagline {
  font-size: 1.5rem;
  font-weight: 500;
  color: #4E342E;
  margin: 0 0 1rem 0;
  line-height: 1.6;
  text-shadow: 0 1px 10px rgba(255, 255, 255, 0.6);
  max-width: 90%;
}

.description {
  font-size: 1.1rem;
  color: #6B5B47;
  margin: 0;
  line-height: 1.6;
}

.hero-logo {
  display: flex;
  justify-content: center;
  align-items: center;
  position: relative;
  animation: fadeInRight 0.8s ease-out 0.2s both;
}

@keyframes fadeInRight {
  from {
    opacity: 0;
    transform: translateX(30px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

.hero-logo::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 120%;
  height: 120%;
  background: radial-gradient(circle, rgba(0, 105, 148, 0.1) 0%, transparent 70%);
  border-radius: 50%;
  z-index: -1;
  animation: pulse 3s ease-in-out infinite;
}

@keyframes pulse {
  0%, 100% {
    opacity: 0.5;
    transform: translate(-50%, -50%) scale(1);
  }
  50% {
    opacity: 0.8;
    transform: translate(-50%, -50%) scale(1.1);
  }
}

.hero-logo-img {
  width: 100%;
  max-width: 900px;
  height: auto;
  filter: drop-shadow(0 8px 24px rgba(0, 0, 0, 0.15));
  transition: transform 0.3s ease;
}

.hero-logo-img:hover {
  transform: scale(1.02);
}

/* Tablet and medium screen adjustments */
@media (max-width: 1024px) {
  .hero-content {
    grid-template-columns: 1fr;
    gap: 2.5rem;
    text-align: center;
  }

  .hero-text {
    text-align: center;
  }

  .hero-title {
    font-size: 5rem;
  }

  .tagline {
    font-size: 1.3rem;
  }

  /* Keep side-by-side layout on tablets, only stack on smaller screens */
  .feature-with-image {
    gap: 4rem;
  }

  .feature-section h2 {
    font-size: 2.2rem;
  }

  .feature-section p {
    font-size: 1.2rem;
  }

  .content-sections .container {
    gap: 5rem;
    padding: 5rem 2rem;
  }
}

@media (max-width: 768px) {
  .hero-content {
    grid-template-columns: 1fr;
    gap: 2rem;
    text-align: center;
    padding: 0 1rem;
  }

  .hero-text {
    text-align: center;
  }

  .hero-title {
    font-size: 4rem;
    letter-spacing: -0.05em;
  }

  .hero-buttons {
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    margin-top: 2rem;
  }

  .btn-primary, .btn-secondary {
    width: 100%;
    max-width: 280px;
    padding: 1rem 1.5rem;
    text-align: center;
  }

  .tagline {
    font-size: 1.2rem;
    max-width: 100%;
  }

  .hero-logo-img {
    max-width: 280px;
  }

  .feature-with-image {
    grid-template-columns: 1fr;
    gap: 2.5rem;
    text-align: center;
    max-width: 100%;
    padding: 0 1rem;
  }

  .feature-content {
    text-align: center;
    order: 1;
  }

  .feature-image,
  .feature-visual {
    order: 2;
  }

  /* On smaller screens, always show content first, even for alternate layout */
  .feature-alternate .feature-visual,
  .feature-alternate .feature-image {
    order: 2;
  }

  .feature-alternate .feature-content {
    order: 1;
  }

  .feature-section h2 {
    font-size: 2rem;
    text-align: center;
    line-height: 1.3;
  }

  .feature-section p {
    font-size: 1.1rem;
    text-align: center;
    line-height: 1.7;
  }

  .feature-image img {
    max-width: 85%;
  }

  .feature-image video {
    max-width: 95%;
  }

  .cli-image {
    max-width: 90% !important;
  }

  .content-sections .container {
    gap: 4rem;
    padding: 4rem 1.5rem;
  }

  .next-steps-buttons {
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .next-steps-buttons .btn-primary,
  .next-steps-buttons .btn-secondary {
    width: 100%;
    max-width: 300px;
    text-align: center;
  }
}

@media (max-width: 480px) {
  .hero {
    padding: 8rem 0 8rem 0;
  }

  .hero-content {
    gap: 1.5rem;
    padding: 0 0.5rem;
  }

  .hero-title {
    font-size: 2.8rem;
    letter-spacing: -0.03em;
    line-height: 1.1;
  }

  .hero-title::after {
    width: 80px;
    height: 3px;
    bottom: -8px;
  }

  .tagline {
    font-size: 1rem;
    line-height: 1.5;
    margin: 0 0 0.5rem 0;
  }

  .hero-buttons {
    margin-top: 1.5rem;
    gap: 0.75rem;
  }

  .btn-primary, .btn-secondary {
    padding: 0.9rem 1.25rem;
    font-size: 0.9rem;
    border-radius: 10px;
    max-width: 260px;
  }

  .hero-logo-img {
    max-width: 240px;
  }
}

@media (max-width: 360px) {
  .hero {
    padding: 6rem 0 6rem 0;
  }

  .hero-title {
    font-size: 2.2rem;
    letter-spacing: -0.02em;
  }

  .tagline {
    font-size: 0.95rem;
  }

  .btn-primary, .btn-secondary {
    padding: 0.85rem 1rem;
    font-size: 0.85rem;
    max-width: 240px;
  }

  .hero-logo-img {
    max-width: 200px;
  }
}

@media (max-width: 480px) {
  .feature-with-image {
    gap: 2rem;
    padding: 0 0.5rem;
  }

  .feature-section h2 {
    font-size: 1.6rem;
    line-height: 1.25;
    margin-bottom: 1rem;
  }

  .feature-section p {
    font-size: 1rem;
    line-height: 1.6;
  }

  .content-sections .container {
    gap: 3rem;
    padding: 3rem 1rem;
  }

  .feature-image img {
    max-width: 90%;
  }

  .cli-image {
    max-width: 95% !important;
  }

  .feature-image-cli,
  .feature-image-video {
    padding: 1rem;
    max-width: 100%;
  }

  .next-steps h2 {
    font-size: 1.8rem;
  }

  .next-steps p {
    font-size: 1rem;
  }

  .next-steps-buttons .btn-primary,
  .next-steps-buttons .btn-secondary {
    max-width: 280px;
    padding: 0.9rem 1.5rem;
    font-size: 0.9rem;
  }
}

@media (max-width: 360px) {
  .feature-with-image {
    gap: 1.5rem;
    padding: 0 0.25rem;
  }

  .feature-section h2 {
    font-size: 1.4rem;
    line-height: 1.2;
  }

  .feature-section p {
    font-size: 0.95rem;
    line-height: 1.5;
  }

  .content-sections .container {
    gap: 2.5rem;
    padding: 2.5rem 0.75rem;
  }

  .feature-image-cli,
  .feature-image-video {
    padding: 0.75rem;
    border-radius: 16px;
  }

  .cli-image {
    border-radius: 12px;
  }

  .feature-video {
    border-radius: 12px;
  }

  .next-steps h2 {
    font-size: 1.6rem;
  }

  .next-steps-buttons .btn-primary,
  .next-steps-buttons .btn-secondary {
    max-width: 260px;
    padding: 0.85rem 1.25rem;
    font-size: 0.85rem;
  }

  .processing-circle {
    border-radius: 20px;
    padding: 1.5rem;
  }

  .processing-group {
    flex-direction: column;
    gap: 1rem;
    align-items: center;
  }

  .processing-group .flow-arrow {
    transform: rotate(90deg);
    font-size: 1rem;
  }

  .flow-step {
    min-width: 80px;
    padding: 1rem;
  }

  .flow-icon {
    font-size: 2rem;
  }

  .flow-label {
    font-size: 0.8rem;
  }

  .flow-image {
    width: 36px;
    height: 36px;
  }

  .feature-badges {
    flex-direction: column;
    gap: 0.75rem;
    align-items: center;
  }

  .badge {
    padding: 0.6rem 1.25rem;
    font-size: 0.9rem;
    text-align: center;
    min-width: 120px;
  }

  .badge-emphasized {
    padding: 0.55rem 1.1rem;
    font-size: 0.85rem;
    min-width: 110px;
  }

  .arrow-to-badge {
    height: 15px;
  }
}

@media (max-width: 480px) {
  .processing-flow {
    padding: 1.5rem 0.75rem;
    gap: 0.75rem;
  }

  .processing-circle {
    padding: 1.25rem;
    border-radius: 16px;
  }

  .processing-group {
    gap: 0.75rem;
  }

  .processing-group .flow-step {
    padding: 0.75rem;
    min-width: 70px;
  }

  .processing-group .flow-image {
    width: 28px;
    height: 28px;
  }

  .processing-group .flow-label {
    font-size: 0.7rem;
  }

  .processing-group .flow-arrow {
    font-size: 0.9rem;
  }

  .arrow-down {
    height: 30px;
  }

  .storage-step {
    padding: 0.6rem !important;
    min-width: 65px !important;
  }

  .storage-step .flow-image {
    width: 24px !important;
    height: 24px !important;
  }

  .arrow-from-storage {
    height: 25px;
  }

  .feature-badges {
    gap: 0.5rem;
    flex-wrap: wrap;
    justify-content: center;
  }

  .badge {
    padding: 0.5rem 1rem;
    font-size: 0.8rem;
    min-width: 100px;
    flex: 1;
    min-width: calc(50% - 0.25rem);
    text-align: center;
  }

  .badge-emphasized {
    padding: 0.45rem 0.9rem;
    font-size: 0.75rem;
    min-width: calc(50% - 0.25rem);
  }
}

@media (max-width: 360px) {
  .processing-flow {
    padding: 1.25rem 0.5rem;
  }

  .processing-circle {
    padding: 1rem;
  }

  .processing-group .flow-step {
    padding: 0.6rem;
    min-width: 60px;
  }

  .processing-group .flow-image {
    width: 24px;
    height: 24px;
  }

  .processing-group .flow-label {
    font-size: 0.65rem;
  }

  .storage-step .flow-image {
    width: 20px !important;
    height: 20px !important;
  }

  .badge {
    padding: 0.45rem 0.8rem;
    font-size: 0.75rem;
    width: 100%;
    margin: 0 auto;
  }

  .badge-emphasized {
    padding: 0.4rem 0.75rem;
    font-size: 0.7rem;
    width: 100%;
  }

  .feature-badges {
    gap: 0.4rem;
    flex-direction: column;
    align-items: stretch;
  }
}
</style>
