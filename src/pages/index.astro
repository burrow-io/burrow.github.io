---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
---

<Layout title="Burrow - Home" description="Burrow project case study and documentation">
  <Navigation />
  <main class="gradient-background">
    <div class="container">
      <section class="hero">
        <div class="hero-content">
          <div class="hero-text">
            <h1 class="hero-title">Burrow</h1>
            <p class="tagline">
              A self-hosted data ingestion pipeline and retrieval API that streamlines and accelerates the development of RAG applications
            </p>
            <div class="hero-buttons">
              <a href="/case-study" class="btn-primary">View Case Study</a>
              <a href="/case-study" class="btn-secondary">Read Docs</a>
            </div>
          </div>
          <div class="hero-logo">
            <img src={`${import.meta.env.BASE_URL}nut-with-doc.png`} alt="Burrow Logo" class="hero-logo-img" />
          </div>
        </div>
      </section>
    </div>
  </main>

  <!-- White background sections -->
  <section class="content-sections">
    <div class="container">
      <section class="feature-section">
        <h2>Self-hosted serverless architecture</h2>
        <p>
          Automated deployment to your AWS environment. Performance and cost scale based on usage patterns.
        </p>
      </section>

      <section class="feature-section">
        <h2>Automated Ingestion Pipeline</h2>
        <p>
          Ease of getting documents into a database via both friendly GUI and RESTful endpoints, the pipeline takes care of the rest.
        </p>
      </section>

      <section class="feature-section">
        <h2>State-of-the-art document processing</h2>
        <p>
          Layout-aware parsing along with intelligent chunking that preserves document structure using the Docling library.
        </p>
      </section>

      <section class="feature-section">
        <h2>Production-grade retrieval</h2>
        <p>
          A RAG API with togglable parameters for hybrid search, reranking, and metadata filtering.
        </p>
      </section>

      <section class="feature-section next-steps">
        <h2>Next Steps</h2>
        <p>
          Ready to dive deeper into Burrow? Explore our comprehensive case study or get started with the documentation.
        </p>
        <div class="next-steps-buttons">
          <a href="/case-study" class="btn-primary">Case Study - Learn How We Built Burrow</a>
          <a href="/case-study" class="btn-secondary">Get Started</a>
        </div>
      </section>
    </div>
  </section>
</Layout>

<style>
.hero-buttons {
  display: flex;
  gap: 1.25rem;
  justify-content: flex-start;
  margin-top: 2.5rem;
}

.btn-primary, .btn-secondary {
  padding: 1rem 2.25rem;
  text-decoration: none;
  border-radius: 12px;
  font-weight: 600;
  font-size: 1rem;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
  box-shadow: 0 4px 14px rgba(0, 0, 0, 0.1);
}

.btn-primary {
  background: linear-gradient(135deg, var(--brand-teal) 0%, #0080a8 100%);
  color: white;
  border: none;
}

.btn-primary::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
  transition: left 0.5s;
}

.btn-primary:hover::before {
  left: 100%;
}

.btn-primary:hover {
  transform: translateY(-3px);
  box-shadow: 0 8px 24px rgba(0, 105, 148, 0.4);
}

.btn-secondary {
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(10px);
  border: 2px solid rgba(0, 105, 148, 0.3);
  color: var(--text-dark);
}

.btn-secondary:hover {
  background: rgba(0, 105, 148, 0.1);
  border-color: var(--brand-teal);
  transform: translateY(-3px);
  box-shadow: 0 8px 24px rgba(0, 105, 148, 0.2);
}

/* Content sections */
.content-sections {
  background: linear-gradient(
    to bottom,
    rgba(255, 255, 255, 0.9) 0%,
    rgba(255, 255, 255, 1) 30%,
    rgba(255, 255, 255, 1) 100%
  );
  padding: 2rem 0 4rem 0;
}

.feature-section {
  padding: 3rem 0;
  max-width: 800px;
  margin: 0 auto;
  text-align: center;
}

.feature-section h2 {
  font-size: 2.5rem;
  color: var(--text-dark);
  margin-bottom: 1.5rem;
  font-weight: 700;
}

.feature-section p {
  font-size: 1.2rem;
  color: var(--text-muted);
  line-height: 1.7;
  margin: 0;
}

.next-steps {
  border-top: 2px solid rgba(166, 124, 82, 0.1);
  padding-top: 4rem;
}

.next-steps-buttons {
  display: flex;
  gap: 1.5rem;
  justify-content: center;
  margin-top: 2rem;
}

.next-steps-buttons .btn-primary {
  background: var(--brand-teal);
  color: white;
  padding: 1rem 2rem;
  text-decoration: none;
  border-radius: 8px;
  font-weight: 500;
  transition: all 0.2s;
}

.next-steps-buttons .btn-secondary {
  border: 2px solid var(--brand-teal);
  color: var(--text-dark);
  padding: 1rem 2rem;
  text-decoration: none;
  border-radius: 8px;
  font-weight: 500;
  transition: all 0.2s;
}

.next-steps-buttons .btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(0, 105, 148, 0.4);
}

.next-steps-buttons .btn-secondary:hover {
  background: var(--brand-teal);
  color: white;
}

.hero-content {
  display: grid;
  grid-template-columns: 1fr 450px;
  gap: 4rem;
  align-items: center;
  max-width: 1600px;
  margin: 0 auto;
  position: relative;
  z-index: 1;
}

.hero-text {
  text-align: left;
  position: relative;
  animation: fadeInUp 0.8s ease-out;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.hero-text h1 {
  margin-bottom: 1.5rem;
}

.hero-title {
  font-size: 6.5rem;
  font-weight: 485;
  color: #4e342e;
  display: inline-block;
  margin: 0;
  letter-spacing: -0.08em;
  font-family: 'Poppins', sans-serif;
  position: relative;
  text-shadow: 0 2px 20px rgba(78, 52, 46, 0.15);
  transition: transform 0.3s ease;
}

.hero-title::after {
  content: '';
  position: absolute;
  bottom: -12px;
  left: 0;
  width: 120px;
  height: 4px;
  background: linear-gradient(90deg, var(--brand-teal), var(--brand-primary));
  border-radius: 2px;
  opacity: 0.7;
}

.tagline {
  font-size: 1.5rem;
  font-weight: 500;
  color: #4E342E;
  margin: 0;
  line-height: 1.6;
  text-shadow: 0 1px 10px rgba(255, 255, 255, 0.6);
  max-width: 90%;
}

.description {
  font-size: 1.1rem;
  color: #6B5B47;
  margin: 0;
  line-height: 1.6;
}

.hero-logo {
  display: flex;
  justify-content: center;
  align-items: center;
  position: relative;
  animation: fadeInRight 0.8s ease-out 0.2s both;
}

@keyframes fadeInRight {
  from {
    opacity: 0;
    transform: translateX(30px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

.hero-logo::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 120%;
  height: 120%;
  background: radial-gradient(circle, rgba(0, 105, 148, 0.1) 0%, transparent 70%);
  border-radius: 50%;
  z-index: -1;
  animation: pulse 3s ease-in-out infinite;
}

@keyframes pulse {
  0%, 100% {
    opacity: 0.5;
    transform: translate(-50%, -50%) scale(1);
  }
  50% {
    opacity: 0.8;
    transform: translate(-50%, -50%) scale(1.1);
  }
}

.hero-logo-img {
  width: 100%;
  max-width: 800px;
  height: auto;
  filter: drop-shadow(0 8px 24px rgba(0, 0, 0, 0.15));
  transition: transform 0.3s ease;
}

.hero-logo-img:hover {
  transform: scale(1.02);
}

@media (max-width: 768px) {
  .hero-content {
    grid-template-columns: 1fr;
    gap: 2rem;
    text-align: center;
  }

  .hero-text {
    text-align: center;
  }

  .hero-buttons {
    flex-direction: column;
    align-items: center;
  }

  .tagline {
    font-size: 1.1rem;
  }
}
</style>
